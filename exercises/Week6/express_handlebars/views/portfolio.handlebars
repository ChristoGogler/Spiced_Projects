<main>
    {{#each projects}}
        <a href="{{directory}}"><div class="card">
                <img src="{{imgurl}}" alt="{{title}} screenshot" />
                <h2>{{title}}</h2>
                <p>{{description}}</p>

            </div></a>
    {{/each}}

</main>